{
  "name": "serialize-map",
  "version": "0.0.1",
  "main": "index.js",
  "license": "MIT",
  "scripts": {
    "test": "nyc mocha",
    "build": "babel src -d lib"
  },
  "devDependencies": {
    "babel-preset-env": "^1.6.1",
    "chai": "^4.1.2",
    "mocha": "^5.0.0"
  },
  "dependencies": {
    "lodash.topath": "^4.5.2"
  },
  "publishConfig": {
    "registry": "https://registry.npmjs.org/"
  }
}
